spring.application.name=vaux-backend

# PostgreSQL Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/vaux_db
spring.datasource.username=postgres
spring.datasource.password=p9qgp2lq2
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=your_super_secret_jwt_key_change_this_in_production_with_a_long_random_string_12345678
jwt.expiration=86400000

# CORS is configured in WebConfig.java
